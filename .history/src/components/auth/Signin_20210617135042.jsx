import React, { useState } from "react";

export default function Signin() {
  const [inputData, setInputData] = useState({});
  return <div className="container">
      <form on className="white"></form>
  </div>;
}
